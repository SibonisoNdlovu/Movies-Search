import {CircleLoader } from "react-spinners";
import './spinner.scss'

function Spinner() {
  return (
    <section className="spinner">
      <CircleLoader />
    </section>
  );
}
export default Spinner;
